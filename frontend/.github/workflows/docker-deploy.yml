name: Build and Push Docker Image

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v3
      
      - name: 设置Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: 登录到Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: 构建并推送Docker镜像
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ secrets.DOCKER_USERNAME }}/connectwise-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
  deploy:
    needs: [build]  # 等待镜像构建完成
    runs-on: ubuntu-latest
    steps:
      - name: 连接服务器并执行部署脚本
        uses: appleboy/ssh-action@v0.1.10  # SSH 连接工具
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}  # SSH 私钥（免密登录）
          script: |
            # 进入服务器项目目录
            cd ~/connectwise

            # 拉取最新镜像（关键步骤：获取前端最新镜像）
            docker-compose pull

            # 重启容器（自动使用新镜像，保留数据卷）
            docker-compose up -d frontend-service